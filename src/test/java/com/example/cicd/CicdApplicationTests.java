package com.example.cicd;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class CicdApplicationTests {

	@Test
	void contextLoads() {
		// Ce test vérifie uniquement que le contexte de l'application Spring se charge correctement.
		// si le contexte de l'application se charge correctement.
		// Si vous souhaitez ajouter des assertions ou des tests spécifiques,
		// implémentez-les ici. Si ce n'est pas nécessaire, supprimez ce commentaire.
	}

}
